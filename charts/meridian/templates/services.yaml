# API Service
apiVersion: v1
kind: Service
metadata:
  name: {{ include "meridian.fullname" . }}-api
  labels:
    {{- include "meridian.labels" . | nindent 4 }}
    app.kubernetes.io/component: api
spec:
  type: {{ .Values.api.service.type }}
  ports:
    - port: {{ .Values.api.service.port }}
      targetPort: api
      protocol: TCP
      name: http
  selector:
    {{- include "meridian.selectorLabels" . | nindent 4 }}

---
# Web Service
apiVersion: v1
kind: Service
metadata:
  name: {{ include "meridian.fullname" . }}-web
  labels:
    {{- include "meridian.labels" . | nindent 4 }}
    app.kubernetes.io/component: web
spec:
  type: {{ .Values.web.service.type }}
  ports:
    - port: {{ .Values.web.service.port }}
      targetPort: web
      protocol: TCP
      name: http
  selector:
    {{- include "meridian.selectorLabels" . | nindent 4 }}
