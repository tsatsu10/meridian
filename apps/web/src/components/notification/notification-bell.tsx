/**
 * @deprecated Prefer `NotificationCenter` — kept as a stable import path.
 * Re-exports the API-backed notification UI (no demo data).
 */
export { default } from "@/components/shared/notifications/notification-center";
