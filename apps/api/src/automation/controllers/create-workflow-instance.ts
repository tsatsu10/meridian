/**
 * 🔄 Create Workflow Instance Controller
 * @epic-3.1-automation-engine
 */

export const createWorkflowInstance = async (c: any) => {
  return c.json({ message: "Create workflow instance - coming soon" });
}; 
